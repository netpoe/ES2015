'use strict';

let fullName = `${first} ${name}`; 

// Multiline text
let message = `Hello ${username}

We are glad to see you back

The current date is ${new Date()}

And you have been out for ${timeOut['last']}

Don't hesitate on contacting us on ${contactArr[0]}

`;